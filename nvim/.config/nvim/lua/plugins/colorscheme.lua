-- Use my Bone colorscheme (defined in colors/bone.lua) instead of the
-- LazyVim default. Bone is a standalone light theme matching ghostty/kitty/cmux.
return {
  { "LazyVim/LazyVim", opts = { colorscheme = "bone" } },
}
